# Railway/Nixpacks — Node 20 + tránh conflict cache npm
[variables]
NIXPACKS_NODE_VERSION = "20"
NPM_CONFIG_CACHE = "/tmp/npm-cache"

[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["npm ci"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "npm run start:prod"
